### 上下文

读取以下文件，了解当前情况：
1. `{{workspace}}/.dag_context.json` — **本次分配的任务**（目标 feature + DAG 状态）
2. `{{workspace}}/task_list.json` — 完整 feature 列表及状态
3. `{{workspace}}/exec-plan.md` — 执行进度表
4. `{{workspace}}/progress.md` — 上次 session 完成的内容
5. 目标项目中的现有源代码
